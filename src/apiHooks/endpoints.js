import React from 'react';

const endpoints = () => {
  const getBreeds = 'Todos los gatos ';
  const getBreedId = 'Especie especifica ';
  const BreedPic = 'img especifica de la especie ';

  return { getBreeds, getBreedId, BreedPic };
};

export default endpoints;
